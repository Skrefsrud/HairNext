/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // TypeScript support (you already likely have your tsconfig.json set up)
  // No additional config usually needed here for basic TypeScript usage

  // Example of additional, often-used configuration options:

  // Environment Variables
  // env: {
  //   API_URL: process.env.API_URL,
  // },

  // Images (if you are using the built-in Next.js Image component)
  // images: {
  //   domains: ['your-image-domain.com'],
  // },
};

module.exports = nextConfig;
